#include "Physics.h"

namespace Kinemo
{
    bool Physics::CheckBox(const BoundingBox& box)
    {
        return true;
    }

    bool Physics::Raycast(const Vec3& ray)
    {
        return true;
    }
}
