#pragma once

namespace Kinemo {
	namespace GUI {

		class Button
		{
		private:
			// function callbacks
		public:
			//Button(Rect rect, const char* text);
			//Button(Rect rect, Sprite icon);
		};
	}
}